import { Fragment } from "react";
import { Topbar } from "../../components/Topbar";

export const Schedule = ({title}) => (
    <Fragment>
        <Topbar title={title} />
        <p>Schedule</p>
    </Fragment>
)
